# Infraestructura

Este directorio agrupara configuraciones de despliegue y operacion.

Alcance previsto:

- Render para `apps/api`.
- Vercel para `apps/web`.
- Supabase PostgreSQL y Storage.
- Variables de entorno por ambiente.
- Checklists de despliegue.

No se deben guardar secretos reales en este directorio.
