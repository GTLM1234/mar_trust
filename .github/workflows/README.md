# GitHub Actions

Los workflows se agregaran cuando existan comandos estables de build, lint y
tests para `apps/web` y `apps/api`.

Regla del proyecto: ningun workflow debe desplegar a produccion sin pasar por
`develop` y revision previa.
